// Moved to a shared component now that the Control Picture lives under AI Control
// Graph (pick a use case → its picture). Kept as a re-export for any stale imports.
export { default } from "@/components/console/control-picture-card";
